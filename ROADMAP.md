# Werewolf Ville Roadmap

更新日期：2026-06-04

关联需求：本路线阶段对应 `USER_REQUIREMENTS_LEDGER.md` 中的需求编号，每阶段完成时在 commit 中标注 `(refs REQ-XXX)`。

## 原则

- 先稳定工程结构，再修体验 bug，再扩展玩法。
- 玩法设计、bug 修复、项目工具/结构治理分频道推进，互不混写。
- 每个阶段都必须有可验证的闭环和 Git 提交点。
- 本文件只记录阶段路线和设计取舍，不记录零碎 bug 或需求细节。零碎 bug 见 `BUG_BACKLOG.md`，需求细节见 `USER_REQUIREMENTS_LEDGER.md`。

## Phase 0：工程安全与结构治理

目标：让项目以后可以长期迭代，不再因为上下文、运行态文件或大文件耦合导致反复回退。

已完成：

- 初始化 Git 仓库。
- 创建首个基线提交：`82ca541 Initial project baseline`。
- 新增 `PROJECT_CONTINUITY.md`。
- 明文 API key 从 `config.yaml` 移除，改为环境变量占位。
- 忽略日志、缓存、临时脚本和 persona 运行态文件。

待做：

- 将 persona 运行态从 `personas/` 迁移到独立 `runtime/` 目录。
- 建立 `BUG_BACKLOG.md`、`CURRENT_SPRINT.md` 的固定使用规则。
- 制定并执行 `game_engine.py` 拆分计划。

## Phase 1：拆分 `game_engine.py`

目标：先把单体文件拆成可测试模块，再集中修 bug。

建议拆分顺序：

1. 纯数据/序列化与运行态辅助逻辑。
2. 任务系统。
3. 黄昏讨论与投票。
4. 对话流程。
5. 银器目标。
6. NPC 调度与行动执行。
7. 夜晚追杀编排。

每拆一块必须：

- 保持外部 API 行为不变。
- 增加或保留对应测试。
- 跑 `python -m py_compile agent.py game_engine.py llm.py ui/app.py`。
- 跑相关 pytest 子集。
- 提交一次 Git。

## Phase 2：白天体验闭环稳定

目标：第一天白天完整可玩、可观察、不卡死。

范围：

- 开局晨会、克罗介绍、NPC 第一轮发言。
- NPC 散场与日常行动。
- 克罗普通交谈与深挖。
- 右侧任务区、NPC 列表、灯泡、详细记录。
- 气泡布局、层级、计时、中文显示。
- 新游戏清空运行态。

## Phase 3：黄昏讨论、投票、监禁闭环

目标：投票不是直接发生，而是完整狼人杀式流程。

正确流程：

1. 黄昏压暗，全员回到广场环形站位。
2. 第一天固定揭露狼人知识，随后按顺时针逐个讨论，克罗最后由玩家发言。
3. 30 秒全员投票；允许自投与弃票，玩家只控制克罗票。
4. 展示整数票数和投票者头像，最高票自动胜出，平票由克罗票裁决。
5. 克罗押送最高票者到牢房并回办公室。
6. 进入玩家不可操作、只显示匿名阶段进度的夜晚过场。

完整规格见 `docs/superpowers/specs/2026-06-07-dusk-voting-night-transition-design.md`。

## Phase 4：夜晚与狼人行动稳定

目标：双狼夜晚杀人是物理发生的，不是纯文本跳变。

范围：

- 双狼团队每晚总共杀 1 人。
- 狼人移动、目标选择、风险评估、线索生成。
- 不能优先杀克罗。
- 失败/困难杀人会留下更多线索。

## Phase 5：狼人知识推进与银器玩法

目标：玩家能推理“可能是狼人”，而不是系统一开始告诉答案。

范围：

- 第一天只看到野兽抓咬痕。
- 第一天图书馆/书架调查发现狼人可能性。
- 第二天再次出现类似尸体后确认狼人。
- 第二天/第三天收集工具和银饰。
- 第四天制作并使用唯一银子弹。
- 隐藏银质小刀角色夜晚行动。

## Phase 6：正式玩家视角

目标：关闭导演模式，把当前调试信息改为正式玩法信息。

范围：

- 隐藏狼人身份、私有思考、模型名、导演摘要。
- 保留必要的玩家提示：任务、线索灯泡、对话历史、公开投票历史。
